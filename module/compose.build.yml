# Auto-generated by install-stack.sh with ANCROO_LOCAL_BUILD=y
# module.sh does NOT auto-load this file.
# To use: add modules/ancroo-runner/compose.build.yml to COMPOSE_FILE in .env

services:
  ancroo-runner:
    image: ghcr.io/ancroo/ancroo-runner:latest
    build:
      context: ../ancroo-runner
      dockerfile: Dockerfile
      args:
        BUILD_COMMIT: ${BUILD_COMMIT:-dev}
